[build]
builder = "nixpacks"
buildCommand = "cd railway && npm install"

[deploy]
startCommand = "cd railway && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services.veridex-api.volumes]]
name = "logs"
mount_path = "/app/logs"
